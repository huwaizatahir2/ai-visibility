; Copy to inventory.ini and set the app host (the Elastic IP from terraform output).
[app]
ai-visibility ansible_host=203.0.113.10 ansible_user=ubuntu

[app:vars]
ansible_python_interpreter=/usr/bin/python3
